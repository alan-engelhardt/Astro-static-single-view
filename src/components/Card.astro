---
const { data } = Astro.props;
// console.log(data);
---

<article data-id={data.id} class={(data.sund && "sund") || "usund"}>
  <h2>{data.navn}</h2>
  <h3>{data.levested}</h3>
  <a href=`/single/${data.navn}`>Læs mere</a>
</article>

<style>
  article {
    padding: 2em;
    position: relative;
  }
  .blue {
    background-color: blue;
    color: white;
  }

  .usund {
    background-color: red;
  }

  .usund::after {
    content: "USUND!";
    padding: 1em;
    background-color: white;
    color: red;
    position: absolute;
    right: 14px;
    bottom: 10px;
    rotate: 345deg;
    box-shadow: grey -2px 3px 15px;
  }

  .sund {
    background-color: greenyellow;
  }
</style>
